# ============================================================================
# VivahSetu - Environment Configuration (2026)
# ============================================================================
# Essential variables only. No unnecessary keys.
# Frontend: VITE_ prefixed variables only (public)
# Backend: Server-side secrets only (private)
# ============================================================================

# ============================================================================
# FRONTEND - Public Variables Only
# ============================================================================

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_API_URL=http://localhost:4000/api

# ============================================================================
# BACKEND - Private Variables Only
# ============================================================================

NODE_ENV=development
PORT=4000

# Note: Frontend runs on port 5173 (vite dev server)
# Backend runs on port 4000
# Frontend API proxy redirects /api requests to backend at http://localhost:4000

# Supabase (Database, Auth, Realtime - ONLY backend service)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Google Places API (Location search - backend-only proxy)
GOOGLE_PLACES_API_KEY=AIzaSyDxxxxxxxxxxxxxxxxxxxx

# Session & Security
JWT_SECRET=your-jwt-secret-min-32-chars-long
JWT_EXPIRY=7d
CORS_ORIGIN=http://localhost:3000

# Email / SMTP (optional - used for workflows, notifications, custom emails)
# Providers supported: smtp, sendgrid
MAIL_PROVIDER=smtp
EMAIL_FROM="Vivah Setu <no-reply@your-domain.com>"
SMTP_HOST=smtp.your-email.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass
SENDGRID_API_KEY=

# OTP and email verification
OTP_TTL_SECONDS=300
OTP_LENGTH=6

# AI / Third-party APIs
# For OpenAI or similar model providers
AI_PROVIDER=openai
OPENAI_API_KEY=

# Optional geocoding provider (node-geocoder). Default is openstreetmap (no key).
NODE_GEOCODER_PROVIDER=openstreetmap
NODE_GEOCODER_API_KEY=

# Twilio (optional SMS for OTP)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
